# URL of the backend API
VITE_API_BASE_URL=http://localhost:8787

# --- Ads (Phase 3) ---
# Which ad backend renders inside the <AdSlot> containers.
#   placeholder (default) — labelled dashed box, zero network calls.
#   adsense               — real AdSense; falls back to the placeholder
#                           per-slot when the client/slot id is missing
#                           or when the script is blocked.
#   none                  — ad-free build; slots reserve layout space
#                           but render nothing.
VITE_ADS_PROVIDER=placeholder

# AdSense publisher client id, e.g. ca-pub-1234567890123456.
# Leave empty in dev; AdsenseAd falls back to the placeholder and
# warns once. Never commit a real value.
VITE_ADSENSE_CLIENT=

# AdSense slot ids per variant. Leave a variant empty to render the
# placeholder for that variant only. The inline variant is reserved
# and is not rendered anywhere in v1.
VITE_ADSENSE_SLOT_TOP=
VITE_ADSENSE_SLOT_BOTTOM=
VITE_ADSENSE_SLOT_INLINE=

# Non-Personalized Ads. true = NPA mode (no personalisation cookies).
# Recommended for GDPR / child-directed / regulated traffic. See
# PHASE_3_PLAN.md §7.3 for the operator's consent obligations.
VITE_ADSENSE_NPA=false
